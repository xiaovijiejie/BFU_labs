{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "MSVC build (bat)",
      "type": "process",
      "command": "${fileDirname}\\build_msvc.bat",
      "args": ["${file}"],
      "options": { "cwd": "${fileDirname}" },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$msCompile"]
    }
  ]
}